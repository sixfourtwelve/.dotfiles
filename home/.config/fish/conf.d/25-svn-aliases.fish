# svn and cvs aliases. The environment they rely on lives in the untracked
# conf.d/secrets.fish and stays off GitHub.

alias sup='svn up'
alias sc='svn commit'
alias sl='svn log'
alias st='svn status'
alias sd='svn diff'
